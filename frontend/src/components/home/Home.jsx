import { Typography, Box } from '@mui/material';

const Home = () => {
    return (
        <Box>   
            <Typography sx={{fontSize:'30px'}}>MyBookList</Typography>
        </Box>
    );
}
 
export default Home;